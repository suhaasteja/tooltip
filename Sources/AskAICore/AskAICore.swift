import Foundation

/// Marker for the core library. Real logic lands in later stages:
/// pasteboard extraction, panel placement math, prompt templating, LLM client.
public enum AskAICore {
    public static let version = "0.1.0"
}
